# Base URL of the Spring Boot backend
VITE_API_BASE_URL=http://localhost:8080

# Show the one-click demo logins and role switcher (seed accounts).
# Set to "false" for any build you deploy publicly - the demo credentials are then stripped from the bundle.
VITE_ENABLE_DEMO_LOGIN=true
